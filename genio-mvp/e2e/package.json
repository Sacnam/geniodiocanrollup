{
  "name": "genio-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for Genio Knowledge OS",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@types/node": "^20.0.0"
  }
}
